spring:
  datasource:
    url: jdbc:h2:mem:forum_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  seed-data: false
  jwt:
    secret: test-jwt-secret-key-must-be-at-least-32-chars
